<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Fond transparent -->
    <stroke
        android:width="2dp"
    android:color="@android:color/black" /> <!-- Couleur de la bordure -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" />
    <corners android:radius="8dp"/> <!-- Coins arrondis si besoin -->
</shape>
